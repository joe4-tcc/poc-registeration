<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="4dp" />
            <!-- Add stronger padding for better shadow visibility -->
            <padding
                android:left="5dp"
                android:top="5dp"
                android:right="5dp"
                android:bottom="5dp" />
            <!-- Define a solid shadow color -->
            <solid android:color="#20000000" /> <!-- A darker semi-transparent shadow -->
        </shape>
    </item>
</layer-list>
